[build]
builder = "RAILPACK"
buildCommand = "corepack enable && corepack prepare pnpm@11.17.0 --activate && pnpm install --frozen-lockfile && pnpm build:worker"

[deploy]
startCommand = "pnpm --filter @booknowtech/worker start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
